<p style="color: green"><%= notice %></p>

<h1>Photos</h1>
<table id="photos">
  <thead>
  <tr>
    <th>Title</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>
  </thead>
  <tbody>
  <% @photos.each do |photo| %>
    <tr>
      <th><%= photo.title %></th>
      <th><%= link_to "View", photo_path(@user,photo) %></th>
      <th><%= link_to "Edit",edit_photo_path(@user,photo)%></th>
      <th><%= link_to "Delete"%></th>
    </tr>
  <% end %>
  </tbody>


</table>

<%= link_to "New photo", new_photo_path %>
