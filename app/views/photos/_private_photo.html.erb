<div id="<%= dom_id photo %>">
  <%= render 'likes/private_button', photo: photo, like_status: like_status %>
  <%= photo.title %>
  <%= link_to "View", photo_path(photo) %>
  <%= link_to "#{photo.user.username}", view_profile_path(photo.user) %>
</div>
