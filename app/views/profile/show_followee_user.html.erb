<main>
  <%= render "profile/user", user: @user %>
  <div class="team-boxed">
    <div class="container">
      <div class="row people">
        <% @followee.each do |user| %>
          <%= render "profile/followee_user_card", user: user %>
        <% end %>
      </div>
    </div>
  </div>

</main>